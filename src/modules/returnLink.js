module.exports = ({
    id, slug, destination, author, creationDate, modifiedDate, visits, account,
}) => ({
    id,
    slug,
    destination,
    author,
    creationDate,
    modifiedDate,
    visits,
    account,
});
