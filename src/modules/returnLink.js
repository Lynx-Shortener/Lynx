module.exports = ({ id, slug, destination, author, creationDate, modifiedDate, visits, account }) => {
	return {
		id,
		slug,
		destination,
		author,
		creationDate,
		modifiedDate,
		visits,
		account,
	};
};
